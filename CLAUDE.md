# CLAUDE.md

이 저장소의 공용 규칙(빌드·검증·커밋·브랜치·배포)의 **SSOT 는 루트 `AGENTS.md`** 다.
세션/작업 시작 전 [`AGENTS.md`](./AGENTS.md) 를 먼저 읽고 따른다.

- 규칙 변경은 `AGENTS.md` 에서만 한다. 이 파일은 포인터이므로 규칙 본문을 복제하지 않는다
  (drift 금지).
- 전역(사용자 단위) 공용 규칙의 SSOT 는 `~/.codex/AGENTS.md` 다.
