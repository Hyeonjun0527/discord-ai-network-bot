# PLANS.md — 장기 작업 ExecPlan 템플릿

이 파일은 여러 세션에 걸치는 작업을 끊기지 않게 이어가기 위한 **템플릿**이다. 실제 실행 계획은
`docs/nexa/exec-plans/` 아래에 하나의 self-contained Markdown 파일로 만든다.

원칙: 새 세션의 작업자는 해당 ExecPlan 하나만 읽고도 목표, 비목표, 현재 진행 상태, 검증 방법,
롤백 방법을 이해하고 안전하게 이어갈 수 있어야 한다.

## 파일명 규칙

`docs/nexa/exec-plans/YYYYMMDD-짧은-슬러그.md`

예시:

```text
docs/nexa/exec-plans/20260620-nexa-p01-social-identity.md
```

## ExecPlan 템플릿

아래 블록을 복사해 실제 계획 파일로 사용한다.

```markdown
# ExecPlan: <작업명>

## 1. 목표

- 최종적으로 참이어야 하는 상태를 한 문장으로 쓴다.
- 관련 task graph ID, PRD, 이슈, SSOT 파일을 명시한다.

## 2. 비목표

- 이번 계획에서 일부러 하지 않을 일을 쓴다.
- 유사하지만 범위 밖인 작업을 명확히 잘라낸다.

## 3. 현재 기준선

- 시작 브랜치/커밋/주요 변경 파일을 기록한다.
- 기존 동작, 실패 로그, 테스트 결과처럼 현재 상태를 증거 중심으로 적는다.
- 로컬 전용 비밀값은 쓰지 않는다.

## 4. 설계 결정

- 작업 중 내린 결정과 이유를 짧게 남긴다.
- 되돌릴 수 없는 선택, SSOT 변경, 생성물 처리 원칙을 반드시 적는다.

## 5. 실행 단계

| 단계 | 상태 | 산출물 | 검증 |
| --- | --- | --- | --- |
| 1 | TODO | <파일/동작> | <명령/증거> |
| 2 | TODO | <파일/동작> | <명령/증거> |

상태 값은 `TODO`, `IN_PROGRESS`, `DONE`, `BLOCKED`, `VERIFIED` 중 하나를 사용한다.

## 6. 진행 로그

- YYYY-MM-DD HH:mm KST — <무엇을 했는지, 어떤 증거로 확인했는지>

## 7. 검증 계획과 증거

- 실행해야 하는 명령을 정확히 적는다.
- 통과한 명령은 exit code, 핵심 출력, 커버리지/빌드 결과를 남긴다.
- 실패한 명령은 실패 원인과 다음 수정 방향을 남긴다.

## 8. 롤백 계획

- 되돌릴 파일, 생성물 재생성 명령, 배포/런타임 롤백 방법을 적는다.
- 데이터/마이그레이션이 있으면 전방 수정 전략을 별도로 적는다.

## 9. 발견사항

- 작업 중 발견한 버그, 드리프트, 문서 불일치, 추후 분리할 일을 적는다.
- 단, 현재 목표 달성에 필요한 일은 발견사항으로 미루지 말고 실행 단계에 넣는다.

## 10. 재개 지침

- 다음 세션이 첫 번째로 볼 파일/명령을 적는다.
- 현재 `IN_PROGRESS` 단계와 멈춘 이유를 한 문장으로 적는다.
```

## 작성 체크리스트

- 목표와 비목표가 모두 있다.
- 변경 대상 파일과 SSOT가 적혀 있다.
- 진행 단계마다 검증 방법이 있다.
- 이미 실행한 검증은 실제 출력 근거가 있다.
- 롤백 방법이 있다.
- 새 세션이 이 파일 하나만 읽어도 다음 행동을 고를 수 있다.
