.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
*.log
.idea/
*.iml
# IDE/build output dir at module root only — must NOT match source packages named `out`
# (e.g. hexagonal `application/port/out/`).
/out/
