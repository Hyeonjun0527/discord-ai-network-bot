# NEXA-P07-T021 닉네임 변경 temporal fact fixture (synthetic; no real user data).
#
# PROJECT_CONTEXT 의 "코알라" 닉네임 변경 일화를 temporal fact 의 supersession 으로 모델한다.
# 한 사람(가명 p-koala)이 닉네임을 "코알라"에서 "두두"로 바꾼다. 과거/현재 질의가 서로 다른 닉네임을 내야 한다:
#   - 현재 prompt(asOf=now)에는 최신 닉네임("두두")만 기본 제공된다.
#   - 과거 회상(asOf=과거)에는 그 시점의 닉네임("코알라")을 쓸 수 있다.
# supersession(T008): 과거 사실을 물리 삭제하지 않고 validTo 로 닫고 SUPERSEDED 로 둔다 — 둘이 공존한다.
#
# 원문/식별자 원문 없음 — 가명 토큰·닫힌 predicate·구조화 값만(data-categories.md).
schemaVersion: nexa.memory-fixture.v1
fixtureId: nickname-change
title: 코알라→두두 닉네임 변경, 현재/과거 질의와 supersession
guildPseudonym: g-koala-fixture
subject: p-koala
predicate: nickname
# 기준 시각(결정론적 replay). validFrom/validTo 는 이 기준의 상대 오프셋(초).
baseInstant: "2026-01-01T10:00:00Z"
facts:
  # 과거 닉네임: [base, base+1h) 유효 → supersession 으로 닫힘.
  - id: fact-nick-koala
    object: "코알라"
    validFromOffsetSec: 0
    validToOffsetSec: 3600
    status: SUPERSEDED
    confidence: 0.8
    sourceEventIds: ["scene-koala-1"]
  # 현재 닉네임: [base+1h, open) 유효 → 현재 사실.
  - id: fact-nick-dudu
    object: "두두"
    validFromOffsetSec: 3600
    validToOffsetSec: null
    status: ACTIVE
    confidence: 0.8
    sourceEventIds: ["scene-dudu-1"]
# supersession lineage(T008): 어느 fact 가 어느 fact 를 대체했는가.
supersedes:
  - supersededFactId: fact-nick-koala
    supersedingFactId: fact-nick-dudu
queries:
  # 현재 조회(base+2h, recall=false): 현재 retrieval(ACTIVE 만) → 최신 닉네임만 기본 제공.
  - asOfOffsetSec: 7200
    recall: false
    expectObject: "두두"
  # 과거 회상(base+30분, recall=true): 그 시점에 유효했던(닫힌 SUPERSEDED 포함) 이전 닉네임을 회상에 쓸 수 있다.
  - asOfOffsetSec: 1800
    recall: true
    expectObject: "코알라"
